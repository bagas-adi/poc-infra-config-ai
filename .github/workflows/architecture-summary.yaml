name: Architecture Summary

on:
  push:
    branches: [main]
    paths:
      - '**/config/**'
      - '**/terraform/**'
      - '**/ansible/**'

jobs:
  summarize:
    name: Generate Architecture Summary
    runs-on: self-hosted
    environment: Local
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install -r /Users/bagas.pamungkas/projects/poc-task/llm-mac/infra-config-llmops/requirements.txt

      - name: Generate architecture summary
        working-directory: /Users/bagas.pamungkas/projects/poc-task/llm-mac/infra-config-llmops
        run: |
          python -m src.summarizer \
            --output ../poc-infra-config-ai/ARCHITECTURE_SUMMARY.md

      - name: Commit updated summary
        env:
          GITHUB_TOKEN:  ${{ secrets.GH_TOKEN }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add poc-infra-config-ai/ARCHITECTURE_SUMMARY.md
          git diff --cached --quiet || git commit -m "docs: update architecture summary [skip ci]"
          git push
